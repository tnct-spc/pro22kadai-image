use crate::corner_detector::Coordinate;

struct PointChain {
    start: usize,
    goal: usize,
    chain_code: Vec<usize>,
}

fn get_chain_code(point: Coordinate, points: &Vec<Coordinate>) {
    let mut v_old = 0;
    let mut v_new = 0;
}

// 点をバイナリサーチする
fn search_points(point: Coordinate, points: &Vec<Coordinate>) -> usize {
    0
}
